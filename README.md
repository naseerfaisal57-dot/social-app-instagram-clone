# Mini Instagram

Run:

1. Install dependencies
```
npm install
```
2. Create `.env` with `MONGO_URI` and `JWT_SECRET` (optional)
3. Start server
```
npm run dev
```

Open `http://localhost:4000`.
